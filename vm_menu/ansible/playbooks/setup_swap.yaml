---
- name: "Setup swap"
  hosts: localhost
  connection: local
  become: true
  gather_facts: true
  roles:
    - name: Setup swap
      role: geerlingguy.swap
      when: ansible_virtualization_type != 'lxc'
