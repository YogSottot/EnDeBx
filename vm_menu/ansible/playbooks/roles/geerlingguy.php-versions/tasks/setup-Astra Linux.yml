---
- name: Set the correct opcache filename (Ubuntu/Debian).
  ansible.builtin.set_fact:
    php_opcache_conf_filename: "10-opcache.ini"

- name: Ensure dirmngr is installed (gnupg dependency).
  ansible.builtin.apt:
    name: dirmngr
    state: present

# Debian-specific tasks.
- name: Add dependencies for PHP versions (Astra Linux).
  ansible.builtin.apt:
    name:
      - apt-transport-https
      - ca-certificates
    state: present

- name: Add Ondrej Sury's apt key (Astra Linux).
  ansible.builtin.get_url:
    url: https://mirror.yandex.ru/mirrors/packages.sury.org/php/apt.gpg
    dest: /etc/apt/keyrings/deb.sury.org.gpg
    mode: "0644"
    owner: root
    group: root

- name: Add Ondrej Sury's repo (Astra Linux).
  ansible.builtin.apt_repository:
    repo: "deb [arch=amd64 signed-by=/etc/apt/keyrings/deb.sury.org.gpg] https://mirror.yandex.ru/mirrors/packages.sury.org/php bookworm main"
    state: present
  register: php_ondrej_debian_repo

- name: Prefer Sury PHP packages over Astra packages.
  ansible.builtin.copy:
    dest: /etc/apt/preferences.d/100-sury-php.pref
    mode: "0644"
    owner: root
    group: root
    content: |
      Package: php*
      Pin: origin "mirror.yandex.ru"
      Pin-Priority: 1000
  register: php_ondrej_astra_pin

- name: Update apt caches after repo is added (Astra Linux).
  ansible.builtin.apt: update_cache=true
  when:
    - php_ondrej_debian_repo.changed or php_ondrej_astra_pin.changed
  tags: ['skip_ansible_lint']
