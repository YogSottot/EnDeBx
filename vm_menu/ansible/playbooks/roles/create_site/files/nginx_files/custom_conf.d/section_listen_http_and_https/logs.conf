location ~* \.log$ {
  expires -1;
  #satisfy any;
  include bx/custom_ip.conf;
  #deny  all;
  #auth_basic "Private";
  #auth_basic_user_file /etc/nginx/.htpasswd;

  charset utf-8;
  types {
     text/plain log;
  }
}
