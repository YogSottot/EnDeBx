---
- name: Geting SSL Certificate Let`s Encrypt for the site {{ domain }}
  hosts: localhost
  connection: local
  become: true
  gather_facts: true
  roles:
    - role: get_lets_encrypt_certificate
